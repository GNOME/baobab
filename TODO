- gconf schema
- clean baobab.h (move data definiton in .c)
- rework loopdir / loopsearch. Make them non-recursive and use
   gnome-vfs for recursing.
- make a customizable list of ignored mountpoints to ignore
   /proc, /sys, /dev, etc.